
    <div class="flex-wrap pad-all heading-btn-wrapper">
        <div class="half">
        {% if entry.heading4 %}
            {{entry.heading4}}
        {% endif %}
        </div>
        <div class="half flex-wrap ">
            {% embed '_partials/button' %}
                {% block class %}{{buttonClassContrast }} col no-marg-top {% endblock %}
                 {% block title %}View Building Facilities {% endblock %}
            {% endembed %} 
        </div>
    </div>


    {% set slider = craft.entries().section('urbanEnvironmentsSlider').all() %}
    {% for slide in slider %}
    <div>
        {{slide.spaceType[0]}}
        {{slide.spaceType[0].url}}
    </div>

{% endfor %}